# Future Features

- Real-time WebSocket dashboard for live visitor count.
- GeoIP integration to automatically resolve IP addresses to Country/City on the backend if the frontend doesn't provide location.
- Support for custom event tracking (e.g. "Clicked Buy Button").
- Advanced funnel analytics.
